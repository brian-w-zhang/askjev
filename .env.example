# Copy to .env and fill in. .env is gitignored.
# Vercel AI Gateway: Jev (typesafe-ai/jev) ONLY. No other gateway models.
AI_GATEWAY_API_KEY=
# Postgres (local Homebrew Postgres for the MVP; Neon later).
DATABASE_URL=
